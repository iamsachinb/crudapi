COMMANDS:

npm -y init -> initialize a node app with all the default settings set to yes
npm i express -> to install express
git init -> to initialize git
git add .-> to add the files to git
git commit -m "initial_project" -> to save the changes in git 

PACKAGE.JSON
  here if we give like this inside script like dev : "nodemon run server.js" we can use npm run dev like that to run this script
